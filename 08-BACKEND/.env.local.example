# Copy to .env or .env.local for backend development
# Docker Compose service started via start-local-db.ps1
DATABASE_URL=postgresql+psycopg2://appuser:appsecret@localhost:5432/appdb

# Optional: production/staging on Render
RENDER_DATABASE_URL=
