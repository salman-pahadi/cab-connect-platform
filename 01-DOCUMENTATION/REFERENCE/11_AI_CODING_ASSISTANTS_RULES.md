# Cursor Usage Rules

- Generate one module at a time
- Schema first, APIs second, logic last
- Never ask Cursor to build full app
- Review logic before accepting code
- Phase 1 constraints must be respected
